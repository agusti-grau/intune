{
  "Rules": [
    {
      "SettingName": "KernelVersion",
      "Operator": "IsGreaterThanOrEqualTo",
      "DataType": "Version",
      "Operand": "6.11.0",
      "MoreInfoUrl": "https://learn.microsoft.com/en-us/mem/intune/protect/compliance-use-custom-settings",
      "RemediationStrings": [
        {
          "Language": "en_US",
          "Title": "Kernel version is below the minimum required (6.11.0)",
          "Description": "Update the kernel and reboot: sudo apt-get update && sudo apt-get install --install-recommends linux-generic && sudo reboot"
        }
      ]
    }
  ]
}
